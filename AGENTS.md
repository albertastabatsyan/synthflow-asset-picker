# Synthflow Asset Picker Share Build

This is the gitignored nested checkout produced by
`../build_share.py`, for the remote-backed `synthflow-asset-picker` Pages
bundle. It is an active share/recovery checkout, not Synthflow design canon,
the Figma curation ledger, or a design-system source. The curation pipeline and
its read-only-Figma boundary are owned by
`/Users/albert/dev/design/systems/synthflow/figma/curation/README.md`; the
design-root operating contract is `/Users/albert/dev/design/AGENTS.md`.

Keep the existing remote history and generated static bundle intact. Do not
write Figma, alter design canon, expose credentials, push, trigger a Pages or
Worker change, or operate the issue/snapshot automation without the applicable
current authorization. For documentation-only changes, run `git diff --check`;
use the parent curation pipeline's targeted verification when generated bundle
content or picker behavior changes.
